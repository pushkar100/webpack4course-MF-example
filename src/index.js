import redButton from './components/redButton'
import greenButton from './components/greenButton'

const body = document.querySelector('body')

body.appendChild(redButton)
body.appendChild(greenButton)